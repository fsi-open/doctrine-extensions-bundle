---
filter:
    excluded_paths: [vendor/*, Tests/*, bin/*, spec/*]

checks:
    php:
        code_rating: true
        duplication: true

tools:
    php_cpd: true
    php_pdepend:
        excluded_dirs: [vendor]
